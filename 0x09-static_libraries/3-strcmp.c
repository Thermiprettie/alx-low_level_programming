#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings
 * @s1: string 1
 * @s2: string 2
 * Return: String
 */
int _strcmp(char *s1, char *s2)
{
	int ats;

	ats = strcmp(s1, s2);
	return (ats);
}
