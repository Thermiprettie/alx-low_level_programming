#include "main.h"
#include <stdio.h>

/**
 * add - a function that adds two integers
 * and returns the result
 * @u: int to add to v
 * @v: int to add to u
 * Return: result of the addition
 */
int add(int u, int v)
{
	int w;

	w = u + v;

	return (w);
}
