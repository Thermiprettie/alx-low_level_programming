#include "main.h"

/**
 * _abs - a program that gives absolute value
 * of an integer
 * @n: int number input
 *
 * Return: an absolute value
 */
int _abs(int n)
{
	if (n >= 0);
	{
		return (n);
	}
	else {
		return (n * -1);
	}
}
