#include "main.h"
/**
 * _abs - a function that prints
 *
 * the absolute value of an integer
 *
 * @n: the int to print
 *
 * Return: absolute value
 */
int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
